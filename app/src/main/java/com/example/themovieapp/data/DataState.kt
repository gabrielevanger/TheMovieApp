package com.example.themovieapp.data

/**
 * Classe auxiliar do módulo 16: controla leitura dos dados (Loading / Success / Error).
 */
object DataState {

    enum class State {
        Error,
        Loading,
        Success,
    }
}
